# Project Roadmap

[ ] ID: 001: Centralize `LocalStorageAdapter` instantiation in `Home` component using `useMemo`.
[ ] ID: 002: Update `displayUnit` state initialization in `Home` to use the centralized `localStorageAdapter` instance.
[ ] ID: 003: Update `displayTime` state initialization in `Home` to use the centralized `localStorageAdapter` instance.
[ ] ID: 004: Update `sortOrder` state initialization in `Home` to use the centralized `localStorageAdapter` instance.
[ ] ID: 005: Refactor `EditWeightForm` to use the `FormError` component for displaying `weightError`.
[ ] ID: 006: Refactor `EditWeightForm` to use the `FormError` component for displaying `dateError`.