name: CI Quality Check

on:
  pull_request:
    branches: [ "dev", "main", "planning-test" ] # Adicionado planning-test
  push:
    branches: [ "dev" ]

jobs:
  validate:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Run Unit Tests
        run: npm run test

      - name: Build Project (Production Check)
        run: npm run build